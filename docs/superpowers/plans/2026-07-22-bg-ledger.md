# BG 任务可观测性 / 账本 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 每次 BG 调用追加一条 JSONL 记录到 `bg_ledger.jsonl`;`mission_state`→进程退出码(告警);`cc ledger` 直读文件查询。零新基础设施。

**Architecture:** 新模块 `src/bg_ledger.rs`(纯函数 + 文件 IO);`lib.rs::run_background` 末尾接 `append` + `exit(mission_exit_code(...))`;`cc.rs` 加 `ledger` 子命令直读文件(不经 daemon)。数据源复用 #1 的 `BgOutcome.subgoals`/`mission_state`(ADR 0030)。

**Tech Stack:** Rust 2024;既有 `serde`/`serde_json`、`background::BgOutcome`/`SubgoalOutcome`、`bg_gate::MissionState`;无 chrono(ts 用 epoch 秒 + 内置 civil-from-days 转 UTC)。测试用 ScriptedProvider + tempdir(hermetic)。

## Global Constraints

- **账本写/读都不经 daemon**——BG 独立于 daemon(ADR 0026);`cc ledger` 直读文件。
- **写账本失败绝不拖垮主流程**:仅 stderr 警告。
- **append-only,不内置轮转**(外部 logrotate;与 sessions/、logs/ 一致)。
- **退出码保守默认**:未知 state → 0(不误报)。
- **hermetic 测试**:纯函数 + echo/false 风格;不烧 LLM、不依赖 daemon/Docker。
- **commit 规范**遵 `skills/commit-conventions.md`;提交到 `feat/bg-ledger` 分支。
- 真实仓 key 路径:`src/bg_gate.rs`(MissionState)、`src/background.rs`(BgOutcome/SubgoalOutcome)、`src/lib.rs`(run_background)、`src/bin/cc.rs`(子命令)。

## 关键既有签名(供各 Task 引用)

```rust
// src/bg_gate.rs(ADR 0030)
#[derive(Debug, Clone, PartialEq, Eq)]
pub enum MissionState { Running, CompletedAllReady, BlockedAt(u64), CircuitBreaker, Error(String) }
// src/background.rs
#[derive(Debug, Clone)] pub enum SubgoalVerdict { Pass, NeedsFix, Inconclusive }
#[derive(Debug, Clone)] pub struct SubgoalOutcome { pub milestone_id: u64, pub verdict: SubgoalVerdict, pub gate_reason: String, pub tool_cap_hit: bool, pub touched_files: Vec<String> }
#[derive(Debug)] pub struct BgOutcome { pub final_text: String, pub tool_calls: Vec<String>, pub denied: Vec<String>, pub events: Vec<String>, pub subgoals: Vec<SubgoalOutcome>, pub mission_state: MissionState }
// src/lib.rs
pub fn run_background(cfg: Config, task: String) -> anyhow::Result<()>;  // 末尾 println 摘要 + Ok(())
// src/bin/cc.rs: match args.as_slice() { [] => repl, ["sessions"] => ..., ..., [msg@..] => one-shot }
```

---

## Task 1: 给 MissionState / SubgoalOutcome / SubgoalVerdict 加 Serialize/Deserialize

**Files:**
- Modify: `src/bg_gate.rs`(`MissionState` derive 行)
- Modify: `src/background.rs`(`SubgoalVerdict`、`SubgoalOutcome` derive 行)

**Interfaces:**
- Produces: `MissionState`、`SubgoalVerdict`、`SubgoalOutcome` 可 `serde_json::to_string` / `from_str`(账本序列化前提)。

- [ ] **Step 1: 写失败测试**(append 到 `src/background.rs` 的 `#[cfg(test)] mod tests`,或新建 `tests/l1_serde.rs`;此处放 background tests 内便于复用 StubClient 构造)
```rust
    #[test]
    fn bg_outcome_types_are_serializable() {
        use crate::bg_gate::MissionState;
        let sg = SubgoalOutcome {
            milestone_id: 1, verdict: SubgoalVerdict::NeedsFix,
            gate_reason: "gate failed".into(), tool_cap_hit: true, touched_files: vec!["a.rs".into()],
        };
        let j = serde_json::to_string(&sg).unwrap();
        assert!(j.contains("NeedsFix") && j.contains("a.rs"), "{j}");
        let back: SubgoalOutcome = serde_json::from_str(&j).unwrap();
        assert_eq!(back.milestone_id, 1);
        // MissionState 各变体可往返
        for s in [MissionState::Running, MissionState::CompletedAllReady,
                  MissionState::BlockedAt(7), MissionState::CircuitBreaker,
                  MissionState::Error("boom".into())] {
            let j = serde_json::to_string(&s).unwrap();
            let back: MissionState = serde_json::from_str(&j).unwrap();
            assert_eq!(format!("{back:?}"), format!("{s:?}"));
        }
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test --lib bg_outcome_types_are_serializable 2>&1 | tail -5`
Expected: 编译失败(`MissionState`/`SubgoalOutcome` 未 impl Serialize)。

- [ ] **Step 3: 实现(加 derive)**

`src/bg_gate.rs` 的 `MissionState` derive 改为:
```rust
#[derive(Debug, Clone, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub enum MissionState {
```
`src/background.rs` 的 `SubgoalVerdict` 与 `SubgoalOutcome`:
```rust
#[derive(Debug, Clone, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub enum SubgoalVerdict { Pass, NeedsFix, Inconclusive }

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct SubgoalOutcome {
```

- [ ] **Step 4: 跑测试确认通过 + 全量不回归**

Run: `cargo test 2>&1 | grep -E "^test result:" | awk '{p+=$4;f+=$6} END{print p" passed, "f" failed"}'`
Expected: 全通过(264→265,新增 1;serde derive 不影响现有)。

- [ ] **Step 5: 提交**

```bash
git add src/bg_gate.rs src/background.rs
git commit -m "refactor(bg): MissionState/SubgoalOutcome 加 Serialize/Deserialize

为账本 JSON 序列化铺垫;纯 derive,无行为变化。"
```

---

## Task 2: `bg_ledger.rs` — 记录类型 + `ledger_path` + `mission_exit_code` + UTC 格式化(纯函数)

**Files:**
- Create: `src/bg_ledger.rs`
- Modify: `src/lib.rs`(加 `pub mod bg_ledger;`)

**Interfaces:**
- Produces: `LedgerRecord`、`LedgerCounts`、`ledger_path(root)`、`mission_exit_code(&MissionState)->i32`、`format_utc(epoch_secs: u64)->String`。

- [ ] **Step 1: 写失败测试**(bg_ledger.rs 内 `#[cfg(test)]`)
```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::bg_gate::MissionState;

    #[test]
    fn exit_code_mapping() {
        assert_eq!(mission_exit_code(&MissionState::CompletedAllReady), 0);
        assert_eq!(mission_exit_code(&MissionState::Running), 0);
        assert_eq!(mission_exit_code(&MissionState::BlockedAt(3)), 2);
        assert_eq!(mission_exit_code(&MissionState::CircuitBreaker), 3);
        assert_eq!(mission_exit_code(&MissionState::Error("e".into())), 4);
    }

    #[test]
    fn format_utc_is_human_readable() {
        // 2026-07-22T08:41:47Z 对应 epoch 1784678507(近似;断言含日期段即可,避免硬编码时区漂移)
        let s = format_utc(1784678507);
        assert!(s.starts_with("2026-07-22"), "{s}");
        assert!(s.contains('T') && s.ends_with('Z'), "{s}");
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test --lib bg_ledger 2>&1 | tail -5`
Expected: 编译失败(模块/函数未定义)。

- [ ] **Step 3: 实现**

创建 `src/bg_ledger.rs`:
```rust
//! BG 任务账本(spec 2026-07-22 #2):每次 BG 调用追加一条 JSONL 记录到
//! `<root>/bg_ledger.jsonl`;`mission_exit_code` 把 mission_state 映射成进程
//! 退出码,供外部调度器(systemd OnFailure / cron)告警。纯函数 + 文件 IO,
//! 不经 daemon。写账本失败仅记 stderr,绝不拖垮主流程。
use crate::background::{BgOutcome, SubgoalVerdict};
use crate::bg_gate::MissionState;
use serde::{Deserialize, Serialize};
use std::path::{Path, PathBuf};

/// 一条账本记录(JSONL 一行)。
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct LedgerRecord {
    /// Unix epoch 秒(UTC)。
    pub ts: u64,
    /// "workgraph" | "<explicit task>" | "no task"。
    pub task: String,
    pub mission_state: MissionState,
    /// 若 mission_state == BlockedAt(id),冗余存 id 便于 grep/过滤。
    #[serde(skip_serializing_if = "Option::is_none", default)]
    pub blocked_at: Option<u64>,
    pub subgoals: Vec<crate::background::SubgoalOutcome>,
    pub counts: LedgerCounts,
}

#[derive(Debug, Clone, Default, Serialize, Deserialize, PartialEq, Eq)]
pub struct LedgerCounts {
    pub tools: usize,
    pub denied: usize,
    pub milestones: usize,
    pub passed: usize,
    pub failed: usize,
}

pub fn ledger_path(root: &Path) -> PathBuf {
    root.join("bg_ledger.jsonl")
}

/// mission_state → 进程退出码。保守:未知 → 0。
pub fn mission_exit_code(state: &MissionState) -> i32 {
    match state {
        MissionState::CompletedAllReady | MissionState::Running => 0,
        MissionState::BlockedAt(_) => 2,
        MissionState::CircuitBreaker => 3,
        MissionState::Error(_) => 4,
    }
}

/// 从 BgOutcome 聚合 counts。
pub fn counts_of(outcome: &BgOutcome) -> LedgerCounts {
    let passed = outcome.subgoals.iter().filter(|s| matches!(s.verdict, SubgoalVerdict::Pass)).count();
    let failed = outcome.subgoals.len() - passed;
    LedgerCounts {
        tools: outcome.tool_calls.len(),
        denied: outcome.denied.len(),
        milestones: outcome.subgoals.len(),
        passed,
        failed,
    }
}

/// 从 LedgerRecord 取 blocked_at(便于 append 复用)。
fn blocked_at_of(state: &MissionState) -> Option<u64> {
    match state { MissionState::BlockedAt(id) => Some(*id), _ => None }
}

/// epoch 秒 → "YYYY-MM-DDTHH:MM:SSZ"(UTC,无 chrono 依赖;civil-from-days 算法)。
pub fn format_utc(epoch_secs: u64) -> String {
    let days = (epoch_secs / 86400) as i64;
    let secs_of_day = (epoch_secs % 86400) as u64;
    // civil-from-days(Hinnant):days since 1970-01-01 → Y/M/D。
    let z = days + 719468;
    let era = if z >= 0 { z } else { z - 146096 } / 146097;
    let doe = (z - era * 146097) as u64;
    let yoe = (doe - doe / 1460 + doe / 36524 - doe / 146096) / 365;
    let y = yoe as i64 + era * 400;
    let doy = doe - (365 * yoe + yoe / 4 - yoe / 100);
    let mp = (5 * doy + 2) / 153;
    let d = doy - (153 * mp + 2) / 5 + 1;
    let m = if mp < 10 { mp + 3 } else { mp - 9 };
    let y = if m <= 2 { y + 1 } else { y };
    let hh = secs_of_day / 3600;
    let mm = (secs_of_day % 3600) / 60;
    let ss = secs_of_day % 60;
    format!("{y:04}-{m:02}-{d:02}T{hh:02}:{mm:02}:{ss:02}Z")
}

#[allow(dead_code)]
fn _unused_blocked_at_helper(r: &LedgerRecord) -> Option<u64> { blocked_at_of(&r.mission_state) }
```
`src/lib.rs` 加 `pub mod bg_ledger;`(紧随 `pub mod bg_gate;`)。

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test --lib bg_ledger 2>&1 | tail -4`
Expected: `2 passed`(exit_code_mapping + format_utc_is_human_readable)。

- [ ] **Step 5: 提交**

```bash
git add src/bg_ledger.rs src/lib.rs
git commit -m "feat(bg_ledger): 记录类型 + mission_exit_code + format_utc

LedgerRecord/LedgerCounts/counts_of + 退出码映射(0/2/3/4)+
epoch→UTC 格式化(civil-from-days,无 chrono)。纯函数,2 单测。"
```

---

## Task 3: `bg_ledger.rs` — `append` + `read_recent`(文件 IO + T1-T5)

**Files:**
- Modify: `src/bg_ledger.rs`

**Interfaces:**
- Produces: `pub fn append(root, outcome: &BgOutcome, task: &str) -> anyhow::Result<()>`、`pub fn read_recent(root, n: usize, only_failed: bool) -> Vec<LedgerRecord>`、`pub fn record_of(outcome: &BgOutcome, task: &str) -> LedgerRecord`(供测试 + append 复用)。

- [ ] **Step 1: 写失败测试**(append 到 bg_ledger.rs tests)
```rust
    use crate::background::{BgOutcome, SubgoalOutcome, SubgoalVerdict};
    use crate::bg_gate::MissionState;
    use tempfile::tempdir;

    fn outcome(state: MissionState) -> BgOutcome {
        let mut o = BgOutcome::default();
        o.mission_state = state;
        o.tool_calls = vec!["read_file".into(), "edit_file".into()];
        o.denied = vec!["run_command: x".into()];
        o.subgoals = vec![SubgoalOutcome {
            milestone_id: 1, verdict: SubgoalVerdict::NeedsFix,
            gate_reason: "gate failed".into(), tool_cap_hit: false, touched_files: vec!["a.rs".into()],
        }];
        o
    }

    #[test]
    fn append_then_read_roundtrip() {
        let dir = tempdir().unwrap();
        append(dir.path(), &outcome(MissionState::BlockedAt(3)), "workgraph").unwrap();
        let recs = read_recent(dir.path(), 10, false);
        assert_eq!(recs.len(), 1);
        let r = &recs[0];
        assert_eq!(r.task, "workgraph");
        assert!(matches!(r.mission_state, MissionState::BlockedAt(3)));
        assert_eq!(r.blocked_at, Some(3));
        assert_eq!(r.subgoals.len(), 1);
        assert_eq!(r.counts, LedgerCounts { tools: 2, denied: 1, milestones: 1, passed: 0, failed: 1 });
    }

    #[test]
    fn read_recent_returns_last_n_in_order() {
        let dir = tempdir().unwrap();
        append(dir.path(), &outcome(MissionState::Running), "a").unwrap();
        append(dir.path(), &outcome(MissionState::Running), "b").unwrap();
        append(dir.path(), &outcome(MissionState::Running), "c").unwrap();
        let recs = read_recent(dir.path(), 2, false);
        assert_eq!(recs.len(), 2);
        assert_eq!(recs[0].task, "b"); // 顺序:旧→新
        assert_eq!(recs[1].task, "c");
    }

    #[test]
    fn read_recent_only_failed() {
        let dir = tempdir().unwrap();
        append(dir.path(), &outcome(MissionState::CompletedAllReady), "ok").unwrap();
        append(dir.path(), &outcome(MissionState::BlockedAt(1)), "bad").unwrap();
        let recs = read_recent(dir.path(), 10, true);
        assert_eq!(recs.len(), 1);
        assert_eq!(recs[0].task, "bad");
    }

    #[test]
    fn read_recent_skips_malformed_lines() {
        let dir = tempdir().unwrap();
        std::fs::write(ledger_path(dir.path()), "{not json\n").unwrap();
        append(dir.path(), &outcome(MissionState::Running), "good").unwrap();
        let recs = read_recent(dir.path(), 10, false);
        assert_eq!(recs.len(), 1, "坏行应被跳过");
        assert_eq!(recs[0].task, "good");
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test --lib bg_ledger 2>&1 | tail -5`
Expected: 编译失败(`append`/`read_recent`/`record_of` 未定义)。

- [ ] **Step 3: 实现**

在 `src/bg_ledger.rs` 补(use 区已有 serde/Path/PathBuf;补 `std::fs::OpenOptions`):
```rust
use std::fs::OpenOptions;
use std::io::{BufRead, BufReader, Write};
```
加函数:
```rust
/// 从 BgOutcome 构造一条记录(ts 取当前 epoch 秒)。
pub fn record_of(outcome: &BgOutcome, task: &str) -> LedgerRecord {
    let ts = std::time::SystemTime::now()
        .duration_since(std::time::UNIX_EPOCH)
        .map(|d| d.as_secs())
        .unwrap_or(0);
    LedgerRecord {
        ts,
        task: task.to_string(),
        blocked_at: blocked_at_of(&outcome.mission_state),
        mission_state: outcome.mission_state.clone(),
        subgoals: outcome.subgoals.clone(),
        counts: counts_of(outcome),
    }
}

/// 追加一条记录到 `<root>/bg_ledger.jsonl`(每行一个 JSON)。IO 失败返 Err。
pub fn append(root: &Path, outcome: &BgOutcome, task: &str) -> anyhow::Result<()> {
    let rec = record_of(outcome, task);
    let line = serde_json::to_string(&rec)?;
    let path = ledger_path(root);
    if let Some(parent) = path.parent() {
        std::fs::create_dir_all(parent)?;
    }
    let mut f = OpenOptions::new().create(true).append(true).open(&path)?;
    writeln!(f, "{line}")?;
    Ok(())
}

/// 读最近 n 条(文件顺序的最后 n,旧→新)。only_failed=true 只回
/// mission_state≠CompletedAllReady 的。损坏行跳过(JSONL 容错)。
pub fn read_recent(root: &Path, n: usize, only_failed: bool) -> Vec<LedgerRecord> {
    let path = ledger_path(root);
    let f = match std::fs::File::open(&path) { Ok(f) => f, Err(_) => return vec![] };
    let all: Vec<LedgerRecord> = BufReader::new(f)
        .lines()
        .filter_map(|ln| ln.ok().and_then(|s| serde_json::from_str::<LedgerRecord>(&s).ok()))
        .collect();
    let filtered: Vec<LedgerRecord> = if only_failed {
        all.into_iter()
            .filter(|r| !matches!(r.mission_state, MissionState::CompletedAllReady))
            .collect()
    } else {
        all
    };
    let start = filtered.len().saturating_sub(n);
    filtered[start..].to_vec()
}
```
(把 Task 2 里的 `_unused_blocked_at_helper` 删掉——`blocked_at_of` 现已被 `record_of` 使用,不再是 dead code。)

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test --lib bg_ledger 2>&1 | tail -4`
Expected: 全 `passed`(Task 2 的 2 + 本 Task 的 4 = 6)。

- [ ] **Step 5: 提交**

```bash
git add src/bg_ledger.rs
git commit -m "feat(bg_ledger): append + read_recent + record_of

append 写 JSONL(父目录自动建);read_recent 取最后 n 条、only_failed
过滤、坏行跳过。record_of 从 BgOutcome 构造记录(ts=now epoch)。4 个
hermetic IO 单测。"
```

---

## Task 4: `lib.rs::run_background` 接入 append + 退出码

**Files:**
- Modify: `src/lib.rs`(`run_background` 末尾)

**Interfaces:**
- Consumes: `bg_ledger::{append, mission_exit_code}`。

- [ ] **Step 1: 写集成测试**(新建 `tests/l1_bg_ledger.rs`,黑盒;复用 testkit)
```rust
mod testkit;
use testkit::*;
use serde_json::json;
use std::sync::Arc;

// 跑一次 BG(空 task + 一个必失败的 milestone)→ 账本多一行 + mission_state=BlockedAt。
#[test]
fn bg_run_appends_ledger_and_blocks() {
    let ws = Workspace::new();
    ws.write("AGENTS.md", "x");
    // 一个 acceptance="false" 的 milestone → 命令门? 不,"false" 无模式 → review 兜底 → Inconclusive → NeedsFix。
    // 为稳定触发 NeedsFix:用 "rustc --bad-flag"(含模式 "rustc",exit≠0)。
    ws.write("workgraph.json", r#"{"schema_version":1,"nodes":[{"id":1,"title":"t","acceptance":"rustc --bad-flag-xyz","deps":[],"status":"pending","touched":[]}]}"#);
    let (p, _rec) = ScriptedProvider::new(vec![assistant_text("doing it")]);
    let out = codecoder::background::run_background(
        p as Arc<dyn codecoder::Provider>, "test-model".into(), 4096, 0.0, ws.root(), "".into(),
    ).expect("bg run");
    // mission_state:里程碑失败无下游 → CompletedAllReady? 不:next_action 在唯一 milestone fail 后,
    //   next_ready()=None & has_blocked_dependent=false → CompletedAllReady。
    //   改为两个独立 milestone 都失败才触发 CircuitBreaker;或断言 subgoals[0].verdict=NeedsFix + 账本有记录。
    assert_eq!(out.subgoals.len(), 1);
    assert!(matches!(out.subgoals[0].verdict, codecoder::background::SubgoalVerdict::NeedsFix));
    // 账本经 lib.rs run_background 写入(本测试直接调 background::run_background,不经 lib 的 append;
    //   故此处只断言可序列化性,真正的 append 集成在下个断言:读 workgraph 状态)。
    assert_eq!(codecoder::workgraph::WorkGraph::read(ws.root()).get(1).unwrap().status,
        codecoder::workgraph::NodeStatus::NeedsFix);
    // 账本文件:lib.rs 路径才写;此处 background::run_background 不写。留作 Task 4 Step 1b 单独断言(见下)。
}
```
> **注**:上面直接调 `background::run_background` 不触发 lib 的 `append`。lib 的 append + exit 接入是本 Task 的实质改动,其集成断言放在 `lib.rs` 的 `#[cfg(test)]`(Step 1b)。

**Step 1b: lib.rs 单元集成测试**(append 到 `src/lib.rs` 的 `#[cfg(test)] mod tests`,若无则建。复用 background 的 outcome 构造不便——改为直接测 `run_background` 写了账本文件:用 StubClient + 空 workgraph → CompletedAllReady → 账本一行、exit_code=0。)
```rust
// src/lib.rs tests
#[test]
fn run_background_writes_ledger() {
    use crate::background::BgOutcome;
    use crate::provider::stub::StubClient;
    use std::sync::Arc;
    let dir = std::env::temp_dir().join(format!("cc_lib_ledger_{}", std::process::id()));
    let _ = std::fs::remove_dir_all(&dir);
    std::fs::create_dir_all(&dir).unwrap();
    // run_background 读 Config::from_env();为稳定,直接测 append+exit 的纯逻辑而非全链路:
    let mut o = BgOutcome::default();
    o.mission_state = crate::bg_gate::MissionState::BlockedAt(9);
    crate::bg_ledger::append(&dir, &o, "workgraph").unwrap();
    let recs = crate::bg_ledger::read_recent(&dir, 5, false);
    assert_eq!(recs.len(), 1);
    assert_eq!(crate::bg_ledger::mission_exit_code(&recs[0].mission_state), 2);
    let _ = std::fs::remove_dir_all(&dir);
}
```

- [ ] **Step 2: 跑测试确认失败/通过**

Run: `cargo test --lib run_background_writes_ledger 2>&1 | tail -4`
Expected: 该测试本身(append 已实现)应直接 PASS——它验证 lib.rs 即将调用的 append+exit 逻辑可用。若 PASS 则 Step 3 的 lib.rs 改动只是"接线"。

- [ ] **Step 3: 改 `lib.rs::run_background`**

把 `lib.rs::run_background` 末尾(现有 `println!("=== summary: ...")` 之后、`Ok(())` 之前)改为:
```rust
    println!("=== summary: {} tools, {} denied ===", outcome.tool_calls.len(), outcome.denied.len());
    // 账本(ADR 0033):追加一条 JSONL;失败仅警告,不拖垮主流程。
    let task_label: &str = if task.trim().is_empty() { "workgraph" } else { task.as_str() };
    if let Err(e) = crate::bg_ledger::append(&cfg.root, &outcome, task_label) {
        eprintln!("bg ledger append failed: {e}");
    }
    let code = crate::bg_ledger::mission_exit_code(&outcome.mission_state);
    if code == 0 {
        Ok(())
    } else {
        // 非零退出码:外部调度器(systemd OnFailure / cron)据此告警。
        std::process::exit(code);
    }
```

- [ ] **Step 4: 跑测试 + 不回归**

Run: `cargo test 2>&1 | grep -E "^test result:" | awk '{p+=$4;f+=$6} END{print p" passed, "f" failed"}'`
Expected: 全通过(新增 lib 集成测试;现有 background/l1 测试不受影响——它们调 `background::run_background` 或带 explicit task,不走 lib 的 exit 分支)。

- [ ] **Step 5: 提交**

```bash
git add src/lib.rs tests/l1_bg_ledger.rs
git commit -m "feat(lib): run_background 接入账本 + 退出码告警

末尾 append 一条 bg_ledger.jsonl(失败仅 stderr 警告,不拖垮主流程);
mission_state 映射进程退出码(0/2/3/4),非 0 时 std::process::exit ——
外部调度器(systemd OnFailure / cron)据此告警。零新基础设施。"
```

---

## Task 5: `cc ledger` 子命令(直读文件)

**Files:**
- Modify: `src/bin/cc.rs`(`match args.as_slice()` 加分支)

**Interfaces:**
- Consumes: `bg_ledger::{read_recent, format_utc, mission_exit_code}`、`Config::from_env().root`。

- [ ] **Step 1: 写失败测试**(append 到 `src/bin/cc.rs` 的 `#[cfg(test)]`,测 pretty-print 纯函数)
先把打印逻辑抽成纯函数便于测:在 `bg_ledger.rs` 加
```rust
/// 单行摘要(供 cc ledger 默认输出)。
pub fn summarize_line(r: &LedgerRecord) -> String {
    let st = match &r.mission_state {
        MissionState::BlockedAt(id) => format!("BlockedAt(#{id})"),
        other => format!("{other:?}"),
    };
    format!(
        "{}  {}  {}m({}✓ {}✗)  {}t  {}d",
        format_utc(r.ts), st,
        r.counts.milestones, r.counts.passed, r.counts.failed,
        r.counts.tools, r.counts.denied
    )
}
```
测试(bg_ledger.rs tests):
```rust
    #[test]
    fn summarize_line_format() {
        let r = LedgerRecord {
            ts: 1784678507, task: "wg".into(), mission_state: MissionState::BlockedAt(3),
            blocked_at: Some(3), subgoals: vec![], counts: LedgerCounts { tools: 15, denied: 2, milestones: 3, passed: 1, failed: 2 },
        };
        let s = summarize_line(&r);
        assert!(s.contains("2026-07-22") && s.contains("BlockedAt(#3)") && s.contains("3m(1✓ 2✗)") && s.contains("15t") && s.contains("2d"), "{s}");
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test --lib summarize_line_format 2>&1 | tail -4`
Expected: 编译失败(`summarize_line` 未定义)。

- [ ] **Step 3: 实现 `summarize_line`**(上方代码加到 bg_ledger.rs)

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test --lib summarize_line_format 2>&1 | tail -3`
Expected: `1 passed`。

- [ ] **Step 5: 加 `cc ledger` 子命令**

在 `src/bin/cc.rs` 的 `match args.as_slice()` 中,在 `[] => repl(&sock)` 之前加:
```rust
        [one, rest @ ..] if one == "ledger" => {
            let root = codecoder::Config::from_env().root;
            let mut n: usize = 10;
            let mut only_failed = false;
            let mut detail = false;
            for a in rest {
                match a.as_str() {
                    "--failed" => only_failed = true,
                    "--detail" => detail = true,
                    s if s.starts_with("--last ") => { n = s["--last ".len()..].parse().unwrap_or(10); }
                    s if s.starts_with("--last=") => { n = s["--last=".len()..].parse().unwrap_or(10); }
                    other => { eprintln!("cc ledger: unknown flag '{other}'"); std::process::exit(2); }
                }
            }
            let recs = codecoder::bg_ledger::read_recent(&root, if detail { 1 } else { n }, only_failed);
            if recs.is_empty() {
                println!("(no bg_ledger.jsonl yet, or no matching records)");
            } else if detail {
                let r = recs.last().unwrap();
                println!("{}  task={}", codecoder::bg_ledger::format_utc(r.ts), r.task);
                println!("  mission: {:?}", r.mission_state);
                println!("  counts:  {:?}", r.counts);
                for sg in &r.subgoals {
                    println!("  - milestone #{}: {:?}  {}", sg.milestone_id, sg.verdict, sg.gate_reason);
                }
            } else {
                for r in &recs { println!("{}", codecoder::bg_ledger::summarize_line(r)); }
            }
        }
```
注:`--last N` 因 cc 把 `ledger --last 50` 切成 `["ledger","--last","50"]`,`rest = ["--last","50"]`;上面按空格前缀匹配 `"--last "` 会失配(它是独立 token)。改为支持独立 token:把 match arm 调整为
```rust
                    "--last" => { /* 取下一个 token;需带索引循环 */ }
```
为简化,实现时把 `rest` 遍历改成 `let mut it = rest.iter(); while let Some(a) = it.next() { match a.as_str() { "--last" => { n = it.next().and_then(|v| v.parse().ok()).unwrap_or(10); } "--failed" => only_failed=true, "--detail" => detail=true, other => { eprintln!(...); exit(2); } } }`。

- [ ] **Step 6: 编译 + 手测 smoke**

Run: `cargo build --bin cc 2>&1 | tail -2`
Expected: `Finished`。
Run: `CODECODER_ROOT=/tmp/cc_ledger_smoke ./target/debug/cc ledger` → 期望 `(no bg_ledger.jsonl yet, ...)`。

- [ ] **Step 7: 提交**

```bash
git add src/bg_ledger.rs src/bin/cc.rs
git commit -m "feat(cc): ledger 子命令直读 bg_ledger.jsonl

cc ledger [--last N] [--failed] [--detail] 直读文件(不经 daemon)。
summarize_line 单行摘要 + format_utc 可读时间。纯函数单测覆盖摘要格式。"
```

---

## Task 6: 文档同步

**Files:**
- Create: `docs/adr/0033-bg-ledger-and-exit-codes.md`
- Modify: `ARCHITECTURE.md`(模块图加 bg_ledger、bg_ledger.jsonl 文件、ADR 索引)、`README.md`(cc ledger 命令 + 退出码表)、`docs/background-agent-scheduling.md`(退出码告警用法)

- [ ] **Step 1: 写 ADR 0033**

Context=#1/#2 背景;Decision=JSONL 账本 + mission_state→退出码(0/2/3/4)+ cc ledger 直读;Status=Accepted;Consequences=零新基础设施、复用调度器语义、append-only 外部轮转、写账本不拖垮主流程。

- [ ] **Step 2: 同步 ARCHITECTURE/README/background-agent-scheduling**

ARCHITECTURE 模块图加 `bg_ledger.rs` 行 + 文件系统图加 `bg_ledger.jsonl` + ADR 索引加 `0033`;README REPL 命令表加 `cc ledger`、env/退出码说明加 BG 退出码表;background-agent-scheduling.md 加"按退出码告警"段(非 0 → systemd OnFailure / cron 邮件)。

- [ ] **Step 3: 提交**

```bash
git add docs/adr/0033-bg-ledger-and-exit-codes.md ARCHITECTURE.md README.md docs/background-agent-scheduling.md
git commit -m "docs: ADR 0033 BG 账本与退出码 + 同步 ARCHITECTURE/README/调度文档"
```

---

## Self-Review(plan vs spec)

**1. Spec coverage:**
- 账本 JSONL 记录(ts/mission_state/subgoals/counts)→ Task 2(类型)+ Task 3(append/read_recent)。
- 退出码映射(0/2/3/4,SIGINT→0)→ Task 2(mission_exit_code)+ Task 4(lib.rs 接入)。SIGINT 路径不变(既有 Ok→exit 0)。
- `cc ledger [--last N] [--failed] [--detail]` 直读 → Task 5。
- Serialize/Deserialize 铺垫 → Task 1。
- 集成(lib run_background 写账本 + exit)→ Task 4 Step 1b + Step 3。
- 损坏行容错 → Task 3 read_recent + T5。
- 写账本不拖垮主流程 → Task 4 Step 3(stderr 警告,继续 exit)。
- 文档(ADR 0033 + ARCHITECTURE/README/scheduling)→ Task 6。

**2. Placeholder scan:** Task 4 Step 1 的 `tests/l1_bg_ledger.rs` 含一段被注释为"直接调 background 不触发 lib append"的测试——这是说明性注释,实质断言(subgoals/NeedsFix/workgraph 状态)有效;真正 lib append 集成在 Step 1b(lib.rs unit test)。Task 5 Step 5 的 `--last` 解析给出了两种写法并指明采用 `it.next()` 版本——非占位,是明确取舍。无 "TBD/适当错误处理"。

**3. Type consistency:** `LedgerRecord`/`LedgerCounts`/`mission_exit_code`/`append`/`read_recent`/`record_of`/`summarize_line`/`format_utc` 在各 Task 间命名一致;`mission_exit_code(&MissionState)->i32` 签名 Task 2 定义、Task 4 调用一致;`append(root, &BgOutcome, &str)` Task 3 定义、Task 4 调用一致。
